#######
# FIX #
#######

file output > nocolor!
file output > in selects!

another multiple grep "pattern|pattern|pattern"

pgrep?
 
v2 commands
.
.
.

&commandRegexp minimal character handler
reach url handling http:\\index.hu\

sh commands
+ add server

"" and '' handler command  


Node occu:
 select node_name, sum(logical_mb) as DATA_IN_MB, sum(num_files) as NUM_OF_FILES from occupancy group by node_name having min(num_files) >=1 order by data_in_mb desc
